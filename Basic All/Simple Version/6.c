/* purpose : area of circle with  reading input */

#include <stdio.h>

void main()

{
	double radius,area;
	scanf("%lf",&radius);
	area= 3.14 * radius * radius;
	printf("%lf", area);
	
}  